{
  "name": "myrpg",
  "version": "0.1.0",
  "description": "A loop-driven RPG with themed environments, master-student training, combat, and pedometer progression",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
